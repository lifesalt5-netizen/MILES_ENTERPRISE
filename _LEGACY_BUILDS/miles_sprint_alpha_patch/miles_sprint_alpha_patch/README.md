# MILES Sprint Alpha Patch

This patch consolidates `D:\P2GC_Intelligence\MILES_OS` into one executable production host without deleting old modules.

Run in PowerShell:

```powershell
cd D:\P2GC_Intelligence\MILES_OS
powershell -ExecutionPolicy Bypass -File .\APPLY_SPRINT_ALPHA.ps1
npm start
```

Open:

```text
http://localhost:3737
```
