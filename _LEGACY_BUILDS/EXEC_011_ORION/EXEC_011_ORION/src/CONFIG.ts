// ORION config scaffold
// No secrets stored in source code.

export const ORION_CONFIG = {
  dbPathEnv: "ORION_DB_PATH",
  modeEnv: "MILES_CONTROLLED_WRITE_MODE",
  defaultMode: "READ_ONLY",
  auditRequired: true,
  verificationRequired: true
};
