'use strict';
const fs=require('fs'),path=require('path');const APPLY=process.argv.includes('--apply');const file=path.resolve(__dirname,'../StartMilesRemoteExecutionBridge.js');let t=fs.readFileSync(file,'utf8').replace(/^\uFEFF/,'');const before="  SAM_BULK_SCHEMA_AUDIT: ['node', ['SCRIPTS/InspectSamBulkSchema.js']]";const after="  SAM_BULK_SCHEMA_AUDIT: ['node', ['SCRIPTS/InspectSamBulkSchema.js']],\n  SAM_QUALIFIED_UNIVERSE_BUILD: ['node', ['SCRIPTS/BuildSamQualifiedUniverse.js']]";if(t.includes(after)){console.log('SAM_QUALIFIED_UNIVERSE_BRIDGE=ALREADY_CURRENT');process.exit(0);}const n=t.split(before).length-1;if(n!==1)throw new Error('anchor count='+n);t=t.replace(before,after);if(APPLY)fs.writeFileSync(file,t,'utf8');console.log('SAM_QUALIFIED_UNIVERSE_BRIDGE='+(APPLY?'APPLIED':'DRY_RUN_OK'));
