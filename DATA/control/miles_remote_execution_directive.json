{
  "id": "miles-revenue-universe-awarded-coverage-20260901-017",
  "enabled": true,
  "job": "REVENUE_UNIVERSE_RECONCILIATION",
  "requestedBy": "CEO_VIA_CHATGPT",
  "note": "Run the governed full revenue-universe reconciliation on deployed main including PR #441 and PR #442. First account for every ORION contractor row with explicit duplicate/canonical identity handling and require lifecycle accounting integrity. Then calculate from the latest governed USAspending prime+subaward staging artifact: exact unique prime awardee UEIs, exact unique subcontract awardee UEIs, exact UEI overlap between prime and subcontract roles, exact deduped unique awarded UEIs in either role, exact awarded UEIs present in the current governed master, exact awarded UEIs missing from the master, and prime/sub in-master versus missing counts separately. Report the actual current master row count, source date range, source row counts, and any rows lacking canonical identity. Exact counts must use UEI only; normalized legal-name fallback metrics must be separately labeled non-exact and must not inflate exact counts. Do not double-count companies serving in both roles. Explicitly calculate whether the exact awarded UEI union exceeds the current master and by how many. Staging/read-only only: no production ORION mutation, no provider or campaign mutation, no emails, no suppression override, no DNS/B12 changes, no approvals, no queue/history deletion, no destructive Git. Publish fresh runtime evidence and fail closed on source, identity, accounting, extraction, SQLite, or safety failure."
}
